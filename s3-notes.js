// S3 notes keyed by the section titles in the documentation index.
export const s3Notes = {
  'Features of Amazon S3':'S3 combines object storage with storage classes, access controls, lifecycle management, replication, monitoring, and analytics. These features are configured for particular bucket types and workloads; a feature listed here is not automatically enabled in an account.',
  'Storage classes':'Storage classes offer different access, resilience, and retrieval characteristics. S3 Standard serves frequent access; infrequent-access and Glacier classes support less frequently retrieved data. S3 Express One Zone is designed for low-latency access in a single Availability Zone. Check the class-specific documentation when comparing tradeoffs.',
  'Storage management':'Lifecycle rules can transition or expire objects. Versioning retains object variants. Object Lock can prevent protected versions from being changed or deleted for a retention period. Replication copies eligible objects and metadata to configured destinations. Batch Operations applies supported operations across large object sets.',
  'Access management and security':'S3 access controls include IAM policies, bucket policies, access points, Block Public Access, and Object Ownership. The guide recommends policies over ACLs for most modern uses. Bucket type matters: the public-access behavior of a general purpose bucket differs from directory, table, and vector bucket models.',
  'Data processing':'S3 event notifications can start workflows through supported services including SNS, SQS, and Lambda. The guide also describes Object Lambda for transforming data returned to applications; consult its availability notice before choosing it for a new design.',
  'Storage logging and monitoring':'CloudWatch provides operational metrics. CloudTrail records supported API activity, with bucket-level and object-level activity belonging to different event categories. S3 server access logging produces request records. These sources answer different questions and have separate configuration and delivery characteristics.',
  'Analytics and insights':'Storage Lens aggregates storage usage and activity measurements. Storage Class Analysis helps examine access patterns. S3 Inventory reports object metadata for auditing and other workflows. These are different views of stored data, rather than interchangeable activity logs.',
  'Strong consistency':'S3 provides strong read-after-write consistency for object PUT and DELETE requests. A read or list initiated after a successful write can observe the completed change. This does not make every bucket configuration operation strongly consistent or provide a transaction spanning several object keys.',
  'How Amazon S3 works':'A bucket contains objects. An object includes its data and metadata, and its key identifies it within a bucket. Versioning can preserve multiple variants under the same key. Bucket type, Region, access policy, and optional features define how a particular store behaves.',
  'Buckets':'The guide describes general purpose, directory, table, and vector buckets. General purpose buckets support broad object-storage workloads. Directory buckets address particular locality and latency requirements. Table buckets support tabular data, while vector buckets store vector indexes for similarity workloads. Their APIs and access rules differ.',
  'Objects':'An object consists of data and metadata stored in a bucket. Metadata includes system-maintained values and can include user-defined values. A bucket, object key, and—when applicable—version ID identify a specific stored version.',
  'Keys':'An object key is its name within a bucket. The combination of bucket and key identifies the object; version IDs distinguish retained versions when versioning applies. Prefixes in keys support organization and policy scoping.',
  'S3 Versioning':'Versioning retains multiple variants of an object in the same bucket. It can help recover from accidental overwrites or deletions. It is configured per bucket and has distinct behavior when enabled, suspended, or never enabled.',
  'Version ID':'After versioning is enabled, S3 assigns a version ID to new object versions. Objects that existed before versioning was enabled initially have a null version ID. Subsequent writes can create new versions with their own identifiers.',
  'Bucket policy':'A bucket policy is a resource-based IAM policy for a bucket and its objects. It describes permissions through principals, actions, resources, and conditions. Its statements must be considered with other applicable access controls when evaluating a request.',
  'S3 access points':'An access point provides a named endpoint with its own access policy for an underlying supported data source. Access points can simplify policy management for shared datasets, and supported configurations can restrict access through a VPC.',
  'Access control lists (ACLs)':'ACLs are an older mechanism for object and bucket permissions. The Bucket owner enforced Object Ownership setting disables ACLs and uses policies for access management. AWS recommends keeping ACLs disabled for most modern use cases.',
  'Regions':'Choose the AWS Region when creating a bucket. The bucket name and Region cannot later be changed. The guide describes regional data placement and the need to explicitly configure cross-Region transfers or replication where required.',
  'Amazon S3 data consistency model':'Object writes and deletes are strongly consistent, and updates to a single key are atomic. S3 does not provide atomic changes across multiple keys through those object operations. Some bucket configuration changes, including initial versioning enablement, can take time to propagate.',
  'Concurrent applications':'Concurrent writes to the same key require care. Request start times and acknowledgment order do not reliably reveal the order in which S3 processes writes. To determine the resulting value, read after the writes complete. Applications needing additional coordination must provide it.',
  'Related services':'The guide connects S3 to other AWS services, including EC2 for compute, EMR for data processing, and Transfer Family for supported file-transfer protocols. A documentation relationship describes a possible use, not a configured deployment.',
  'Accessing Amazon S3':'S3 can be accessed through the AWS Management Console, CLI, SDKs, or REST API. SDKs handle tasks such as request signing and retries. The access method does not replace authorization: each request still follows the applicable access rules.',
  'AWS Management Console':'The console is a browser-based interface for managing S3 resources. Sign in to the relevant AWS account and identity to work with the resources that identity is permitted to access.',
  'AWS Command Line Interface':'The AWS CLI supports programmatic and interactive management of S3. The s3api and s3control command references describe service operations. Scripts still require appropriately configured credentials and permissions.',
  'AWS SDKs':'AWS SDKs expose S3 APIs in supported programming languages. They handle common request tasks such as signing, retries, and error handling. SDK behavior and feature support depend on the chosen SDK and version.',
  'Amazon S3 REST API':'The REST API exposes S3 operations over HTTP. Applications making direct authenticated API requests need to implement the required request signing. SDKs provide this functionality through their supported interfaces.',
  'Paying for Amazon S3':'S3 pricing is usage-based. Storage, requests, retrieval, and transfer considerations depend on the workload and selected features. Consult the current pricing page rather than treating a stored documentation snapshot as a price quote.',
  'PCI DSS compliance':'The guide states that S3 supports workloads involving payment-card data and is included in AWS PCI DSS validation. The applicable AWS compliance package provides the detailed scope; using an eligible service does not by itself establish an application’s compliance.',
};
